import { render, fireEvent } from '@testing-library/react';
import SendMessage from './SendMessage';

describe('sendMessage', () => {
  it('should get at least one character when click send btn', () => {});
});
